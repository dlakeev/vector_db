Разворачивание базы данных для хранения векторных представлений в PostgreSQL с помощью Docker:<br>

```bash
# Разворачиваем контейнеры
docker-compose up -d

#docker exec для инициализации тестовой базы и загрузке в нее дампа
./init_test_db.sh
```
